{
  "scripts": {
    "esbuild:build": "node --require esbuild-register bundler.ts",
    "sass:build": "sass -I scss_utils src/a.scss"
  },
  "devDependencies": {
    "esbuild": "^0.14.1",
    "esbuild-register": "^3.2.0",
    "esbuild-sass-plugin": "^1.7.1",
    "sass": "^1.44.0"
  }
}
